(defun sirius/init-magit ()
    (if (not sirius-dumped)
        (progn
        
        
        )    
    )
)

(defun sirius/init-magit-base ()
    (use-package magit
        :init


    )
)


(provide 'sirius-magit)